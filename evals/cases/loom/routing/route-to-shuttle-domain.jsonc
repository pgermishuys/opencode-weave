{
  "id": "route-to-shuttle-domain",
  "title": "Routes to Shuttle for domain-specialist framing asks",
  "description": "Verifies that Loom delegates domain-specialist requests (GraphQL types, resolvers, validation) to Shuttle when framed as needing a domain expert.",
  "phase": "routing",
  "tags": ["routing", "loom"],
  "target": {
    "kind": "builtin-agent-prompt",
    "agent": "loom"
  },
  "executor": {
    "kind": "model-response",
    "provider": "github-models",
    "model": "gpt-4o-mini",
    "input": "I need a domain specialist to handle the GraphQL schema generation for our product catalog — types, resolvers, and input validation schemas. This is category-specific specialized work."
  },
  "evaluators": [
    {
      "kind": "llm-judge",
      "expectedContains": ["shuttle"],
      "forbiddenContains": ["I will implement this directly"]
    }
  ]
}
